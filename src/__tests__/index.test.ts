describe('...', () => {
  it('should create an item', () => {
    expect.hasAssertions()
  })
  it('should update an item', () => {
    expect.hasAssertions()
  })
  it('should delete an item', () => {
    expect.hasAssertions()
  })
  it('should get an item by id', () => {
    expect.hasAssertions()
  })
  it('should get an item by name (partial)', () => {
    expect.hasAssertions()
  })
  it('should rename an item', () => {
    expect.hasAssertions()
  })
  it('should change place in a list', () => {
    expect.hasAssertions()
  })
  it('should change parent', () => {
    expect.hasAssertions()
  })
  it('should add comment', () => {
    expect.hasAssertions()
  })
  it('should save everything', () => {
    expect.hasAssertions()
  })
})

describe('...', () => {
  it('should create a note', () => {
    expect.hasAssertions()
  })
  it('should update a note', () => {
    expect.hasAssertions()
  })
  it('should delete a note', () => {
    expect.hasAssertions()
  })
  it('should get a note by id', () => {
    expect.hasAssertions()
  })
  it('should get a note by name (partial)', () => {
    expect.hasAssertions()
  })
  it('should rename an note', () => {
    expect.hasAssertions()
  })
  it('should add item parent', () => {
    expect.hasAssertions()
  })
  it('should remove item parent', () => {
    expect.hasAssertions()
  })
  it('should save everything', () => {
    expect.hasAssertions()
  })
})
